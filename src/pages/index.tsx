import Icon from "@atoms/icons";
import type { NextPage } from "next";

const Home: NextPage = () => {
    return (
        <>
            <Icon name="arrow-down" size={100} />
        </>
    );
};

export default Home;
